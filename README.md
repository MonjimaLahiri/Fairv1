
  # Design settings page features

  This is a code bundle for Design settings page features. The original project is available at https://www.figma.com/design/1xkQjnn9edpw15c5lkj62Q/Design-settings-page-features.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  